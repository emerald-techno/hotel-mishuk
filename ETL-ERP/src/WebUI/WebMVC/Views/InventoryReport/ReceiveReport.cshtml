@model Domain.ViewModel.Report.ReceiveReportVM
@{
    ViewData["Title"] = "Receive Report";
    Layout = "_Layout";
}

<div class="row">
    <div class="col-sm-12 default-according">
        <form>
            <div class="card">
                <div class="card-header p-4">
                    <h5>Receive Report</h5>
                    <div class="setting-list">
                        <input type="button" id="ShowReportBtn" class="btn btn-primary" value="Show Report" />
                    </div>
                </div>
                <div class="card-body table-responsive">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="row">
                                <div class="col-md-2">
                                    <label class="col-form-label">Category</label>
                                    <select asp-for="CategoryId" asp-items="Model.CategoryLookUp" class="form-control dd-type"></select>
                                </div>
                                <div class="col-md-2">
                                    <label class="col-form-label">Item</label>
                                    <select asp-for="ItemId" asp-items="Model.ItemLookUp" class="form-control dd-type"></select>
                                </div>
                                <div class="col-md-2">
                                    <label class="col-form-label">Account</label>
                                    <select asp-for="LedgerId" asp-items="Model.LedgerLookUp" class="form-control dd-type"></select>
                                </div>
                                <div class="col-md-2">
                                    <label class="col-form-label">From Date</label>
                                    <input class="form-control from_dt" asp-for="StrFromDate" placeholder="Enter start date" data-language="en" />
                                </div>
                                <div class="col-md-2">
                                    <label class="col-form-label">To Date</label>
                                    <input asp-for="StrToDate" class="form-control to_dt" placeholder="Enter end date" data-language="en" />
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card">
                <div class="card-header p-4">
                    <h5>Inventory Receive Report</h5>
                    <div class="setting-list">
                        <button class="btn btn-primary" id="ReportPrintBtn">PDF</button>
                    </div>
                </div>
                <div class="card-body">
                    <dvi class="table-responsive">
                        <div style="text-align: center;">
                            <div style="text-align: center; font-weight: bold; font-size: 16px; line-height: 20px">INVENTORY RECEIVE REPORT</div>
                        </div>
                        <div id="ReportContainer"></div>
                    </dvi>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts
{
    <script>
        $(".from_dt").datepicker({
            dateFormat: 'dd/mm/yyyy',
            autoClose: true
        });

        $(".to_dt").datepicker({
            dateFormat: 'dd/mm/yyyy',
            autoClose: true
        });
    </script>

    <script src="~/js/ETL/InventoryReport/ReceiveReport.js"></script>
}